spring:
  datasource:
    url: jdbc:h2:mem:cuenta_movimiento_gatling;MODE=PostgreSQL;DB_CLOSE_DELAY=-1;DATABASE_TO_LOWER=TRUE
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect
        jdbc:
          lob:
            non_contextual_creation: true
  mvc:
    servlet:
      load-on-startup: 1
client:
  persona:
    base-url: http://127.0.0.1:19090
springdoc:
  api-docs:
    enabled: false
  swagger-ui:
    enabled: false
server:
  port: 1204
logging:
  level:
    root: WARN
    org.springframework: WARN
    web: WARN
debug: false
